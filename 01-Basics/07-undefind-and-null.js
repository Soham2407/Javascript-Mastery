let age;

const empty = null;
// undefind
console.log(age);

// null
console.log(empty);
console.log(typeof empty); // object (its bug in js)

// BigInt
const num = BigInt(125632442);
console.log(num);
console.log(typeof num);
